addr: ":1053"
http: ":8080"
ttl: 5m
resolvers:
  # mDNS 桥接示例:把 .local 主机名查询桥到 LAN mDNS(querier-only)。
  # interface 必填且因机器而异,故默认不启用;同机不能跑 avahi/umdns(抢 5353)。
  # - type: mdns
  #   interface: br-lan
  #   ttl: 1m
  #   negative-ttl: 30s
  #   timeout: 1s
  - name: default
    type: forward
    url: 114.114.114.114
